## 1. Grid density

- [x] 1.1 Update loading skeleton grid in `DashboardContent.tsx` to `grid-cols-2 gap-3 sm:gap-4 md:grid-cols-3 lg:grid-cols-5` (drop `landscape:` / `lg:4` / `xl:5`)
- [x] 1.2 Update populated favorites grid in `DashboardContent.tsx` to the same class string

## 2. Verify

- [x] 2.1 Run `npm run typecheck` and `npm run lint` (and `npm run build` before ship)
